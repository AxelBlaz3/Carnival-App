enum WorkoutCategory { PODCAST, SHOW, MOVIE }

enum Status { SUCCESS, LOADING, ERROR }

enum ModelData {title, rating, runtime, episodeCount, seasonCount, imageUrl}

enum PaginationFetchingStatus { idle, loading }